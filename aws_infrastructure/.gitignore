*.swp
package-lock.json
__pycache__
.pytest_cache
.venv
*.egg-info

# CDK asset staging directory
.cdk.staging
cdk.out

# Generated Lambda artifacts (CDK handles bundling automatically)
# Keeping these for backwards compatibility in case old build artifacts exist
layers/
lambda/
